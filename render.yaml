services:
  - type: web
    name: x41-smartlead-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python smartlead_mcp_server.py --transport http --port 10000
    envVars:
      - key: SMARTLEAD_API_KEY
        sync: false
